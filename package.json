{
  "name": "react-scroll-element",
  "version": "2.0.3",
  "description": "React NPM library containing customizable scroll elements.",
  "author": "Cristian Boarna <contact@crisboarna.com>",
  "license": "MIT",
  "private": true,
  "workspaces": ["packages/*"],
  "homepage": "https://crisboarna.github.io/react-scroll-element",
  "repository": {
    "type": "git",
    "url": "https://github.com/crisboarna/react-scroll-element.git"
  },
  "bugs": {
    "url": "https://github.com/crisboarna/react-scroll-element/issues"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "scripts": {
    "prepare": "husky install",
    "lib:lint": "yarn workspace react-scroll-element lint",
    "lib:test": "yarn workspace react-scroll-element test",
    "lib:build": "yarn workspace react-scroll-element build",
    "docs:start": "yarn workspace docs start",
    "docs:build": "yarn workspace docs build",
    "docs:serve": "yarn workspace docs serve",
    "docs:deploy": "yarn workspace docs deploy"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.1.0",
    "@commitlint/config-conventional": "^16.0.0",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^8.0.2",
    "prettier": "^2.5.1"
  }
}
